mixin button-big({text, hasArrow, type, disabled})  
  - const classes = ["button-big"]
  if hasArrow
    - classes.push("button-big_with-arrow")
  if disabled
    - classes.push("button-big_disabled")
  button(class=classes type=type)
    span #{text}
    .button-big__arrow  