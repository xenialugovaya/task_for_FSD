mixin button-big({text, hasArrow, disabled})  
  - let classes = ["button-big"]
  if hasArrow
    - classes.push("button-big_with-arrow")
  if disabled
    - classes.push("button-big_disabled")
  button(class=classes)
    span #{text}
    .material-icons  