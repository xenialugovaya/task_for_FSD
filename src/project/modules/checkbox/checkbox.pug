mixin checkbox({title, name, value, description, rich, checked})
  - const mods = [""]
  if rich
    - mods.push("_rich")
  label(class=mods.map(mod => "checkbox-button" + mod))
    input(class="checkbox-button__input" type="checkbox", name=name, value=value, checked=checked)
    div(class=mods.map(mod => "checkbox-button__title" + mod))= title
      div(class=mods.map(mod => "checkbox-button__description" + mod))= description
