mixin input({placeholder, type, masked, focused})
  - let classes = ['input']
  - let focus = 'false'
  if focused
    - classes.push('input_focused')
  if masked
    - classes.push('js-input_masked')

  input(class=classes type=type placeholder=placeholder)
